;REMOTE SCRIPT START
;while ! Keyevent("q")
	;continue
	
logErr(IDirectDraw4.unhook("EnumDisplayModes"))	
logErr(IDirectdraw4.Hook("Initialize"))
IDirectdraw4_Initialize(p1)
{
	;dllcall(IDirectdraw4.Initialize, ptr, p1)
	return 0
}

AltIDirectDraw4_SetDisplayMode(p1, p2, p3, p4, p5, p6)
{
	r := IDirectDraw4_SetDisplayMode(p1, p2, p3, p4, p5, p6)
	logerr("AltIDirectDraw_SetDisplayMode")
	SetupPrxs4(p1)	
	return r
}
